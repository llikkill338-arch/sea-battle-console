// ============================================================================
// utils.h — Утилиты и глобальные определения
// Модуль: глобальные переменные, звуковые эффекты, константы
// ============================================================================

#ifndef UTILS_H
#define UTILS_H

#include <windows.h>
#include <string>

// --- Константы игры ---
const int R = 10;                    // Размер поля (10x10)
const int SCREEN_WIDTH = 80;         // Ширина консоли в символах
const int SCREEN_HEIGHT = 25;        // Высота консоли в символах
const int DIVIDER_X = 40;            // Позиция вертикального разделителя
const int FLEET_SIZE = 10;           // Количество кораблей во флоте

// --- Перечисление цветов консоли (соответствует WinAPI) ---
enum Color {
    black = 0,        // Чёрный
    blue = 1,         // Синий
    green = 2,        // Зелёный
    cyan = 3,         // Бирюзовый
    red = 4,          // Красный
    magenta = 5,      // Пурпурный
    brown = 6,        // Коричневый
    lightgray = 7,    // Светло-серый
    darkgray = 8,     // Тёмно-серый
    lightblue = 9,    // Светло-синий
    lightgreen = 10,  // Светло-зелёный
    lightcyan = 11,   // Светло-бирюзовый
    lightred = 12,    // Светло-красный
    lightmagenta = 13,// Светло-пурпурный
    yellow = 14,      // Жёлтый
    white = 15        // Белый
};

// --- Значения клеток поля ---
const int CELL_WATER = 0;    // Вода (пусто)
const int CELL_SHIP = 1;     // Корабль (живой)
const int CELL_MISS = 2;     // Промах (стреляли мимо)
const int CELL_HIT = 3;      // Попадание (подбито)

// --- Глобальные переменные ---
extern HANDLE hstdout;      // Дескриптор стандартного вывода консоли
extern bool soundEnabled;   // Флаг включения звука

// --- Функции ---

/**
 * Воспроизведение звукового эффекта через системный динамик.
 * Учитывает глобальный флаг soundEnabled.
 * @param freq — частота в герцах
 * @param duration — длительность в миллисекундах
 */
void playSound(int freq, int duration);

#endif // UTILS_H
